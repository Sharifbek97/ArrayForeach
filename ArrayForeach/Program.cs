var students = new []{"Ali","Vali","Gani"};
foreach (var student in students)
{
    Console.WriteLine(student);    
}

//project of second

var numbers = new []{1,3,4,0,-12};
foreach (var number in numbers)
{
    Console.WriteLine(number);    
}